<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".route.FavouriteRoutesActivity">

    <include
        android:id="@+id/favourite_routes_toolbar"
        layout="@layout/toolbar_driver" />

    <ListView
        android:id="@+id/lvFavouriteRoutes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/favourite_routes_toolbar" />

</androidx.constraintlayout.widget.ConstraintLayout>